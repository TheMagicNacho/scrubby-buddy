# Kani model-checker configuration.
# Enable the experimental async support so that kani::block_on can be used
# in proof harnesses to drive async functions.
# Pass -Z async-lib on the command line (e.g. cargo kani -Z async-lib) or
# via the CI action's `args` parameter.
